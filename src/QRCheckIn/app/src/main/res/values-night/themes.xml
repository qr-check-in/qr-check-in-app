<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QRCheckIn" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/Surface</item>
        <item name="android:textColor">@color/OnSurface</item>
        <item name="hintTextColor">@color/OnSurface</item>
        <item name="android:textColorHint">@color/OnSurface</item>
        <item name="android:windowAnimationStyle">@null</item>
        <!-- Customize your dark theme here. -->
        <item name="android:navigationBarColor">@color/SurfaceContainer</item>
        <item name="android:statusBarColor">@color/SurfaceContainer</item>
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
</resources>